{
  "execMap": {
    "ts": "ROOT_DIR=$(pwd) NODE_PATH=./src ts-node -r dotenv/config dotenv_config_path=../../.env"
  },
  "ext": "ts",
  "ignore": [
    ".git",
    "node_modules",
    "dist",
    "tests",
    "tsconfig.tsbuildinfo"
  ],
  "watch": [
    "src",
    ".env"
  ]
}
